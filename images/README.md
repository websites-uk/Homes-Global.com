# Homes Global image assets

Replace `favicon.png` and `og-image.jpg` with the client's final brand assets when ready.

The property and hero imagery currently loads from Unsplash URLs in `index.html`.